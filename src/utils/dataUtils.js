
// For parsing sensor data (placeholder for future parsing logic)
export const parseSensorData = (rawData) => {
    // Add your parsing logic here later
    return rawData ? rawData.toString(): 'No data'; // Example: Show hex string
  };
  